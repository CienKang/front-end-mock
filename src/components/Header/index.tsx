import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <span>My <b className='bolder'>Record </b> Shelf</span>
        </header>
    );
};

export default Header;